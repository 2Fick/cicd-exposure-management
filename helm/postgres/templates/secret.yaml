# stores sensitive data
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
type: Opaque
stringData:
  POSTGRES_USER: {{.Values.database.user}}
  POSTGRES_PASSWORD: {{randAlphaNum 20 | quote }}
  POSTGRES_DB: {{.Values.database.name}}