# Loads the dashboard from a file outside templates, so helm does not try to interpret the grafana placeholders inside its jsonn
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: {{ .Values.namespace | default .Release.Namespace }}
data:
  exposure.json: |
{{ .Files.Get "dashboards/exposure.json" | indent 4 }}